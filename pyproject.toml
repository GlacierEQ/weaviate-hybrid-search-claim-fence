[project]
name = "weaviate-hybrid-search-claim-fence"
version = "0.1.0"
description = "Deterministic hybrid-search evidence sufficiency and rebinding verifier"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
